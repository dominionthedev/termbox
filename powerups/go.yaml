name: go
description: "Go development tools — lint, test, build helpers"
version: "1.0.0"
scripts:
  - stats
tools: []
rules:
  always: false
  requires:
    - go
  env:
    - kind: file_exists
      value: go.mod
